name: Vibe_Code_Project_1 — CD

on:
  push:
    branches: [main]
    paths:
      - "Vibe_Code_Project_1/**"
  release:
    types: [published]

env:
  REGISTRY: ghcr.io
  IMAGE_NAME: mwyatt9214-cmd/vibe-code-project-1

defaults:
  run:
    working-directory: Vibe_Code_Project_1

jobs:
  deploy:
    name: Build & Push Image
    runs-on: ubuntu-latest

    permissions:
      contents: read
      packages: write

    steps:
      - uses: actions/checkout@v4

      - name: Log in to GitHub Container Registry
        uses: docker/login-action@v3
        with:
          registry: ${{ env.REGISTRY }}
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Extract metadata
        id: meta
        uses: docker/metadata-action@v5
        with:
          images: ${{ env.REGISTRY }}/${{ env.IMAGE_NAME }}
          tags: |
            type=sha,prefix=sha-
            type=ref,event=branch
            type=semver,pattern={{version}}
            type=raw,value=latest,enable=${{ github.ref == 'refs/heads/main' }}

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Build and push
        uses: docker/build-push-action@v5
        with:
          context: ./Vibe_Code_Project_1
          push: true
          tags: ${{ steps.meta.outputs.tags }}
          labels: ${{ steps.meta.outputs.labels }}
          cache-from: type=gha
          cache-to: type=gha,mode=max

  # ── Uncomment to auto-deploy after image push ─────────────────────────────
  # deploy-to-server:
  #   needs: deploy
  #   runs-on: ubuntu-latest
  #   steps:
  #     - name: SSH deploy
  #       uses: appleboy/ssh-action@v1
  #       with:
  #         host: ${{ secrets.SERVER_HOST }}
  #         username: ${{ secrets.SERVER_USER }}
  #         key: ${{ secrets.SSH_PRIVATE_KEY }}
  #         script: |
  #           docker pull ghcr.io/${{ env.IMAGE_NAME }}:latest
  #           docker compose -f ~/Vibe_Code_Project_1/docker-compose.yml up -d --no-deps app
